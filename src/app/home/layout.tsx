export const metadata = {
  title: 'HireNinja Home',
  description: 'AI Recruitment Platform',
}

export default function HomeLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>;
}
