export const metadata = {
  title: 'About HireNinja',
  description: 'AI Recruitment Platform',
}

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>;
}
